# MYCO

> Substrate id: `ask_codex_skill`
> Generated by `myco germinate` on 2026-04-30T15:43:48Z.
> This file is the agent-entry page. Subsequent sessions refine it
> through Ingestion's boot-brief injector; humans do not read it.

## Root principles

1. Only-for-agent — no file in this substrate is authored for a human
   reader; every artifact is consumed by the agent or by tooling on
   the agent's behalf.
2. 永恒吞噬 — the substrate's boundary is not a boundary; anything
   reachable is fair game to ingest.
3. 永恒进化 — the substrate and its contract may evolve; schema and
   version fields track the change.
4. 永恒迭代 — every call is a refinement over the last, not a
   replacement.
5. 万物互联 — nothing stands alone; cross-references are the default.

## Canonical surface

- `_canon.yaml` — single source of truth for this substrate.
- `docs/architecture/` — contract docs (L0/L1/L2/L3).
- `notes/` — raw → digested → integrated → distilled pipeline outputs.
- `.myco_state/` — runtime state; `autoseeded.txt` marks
  skeleton-mode so homeostasis downgrades foundational findings.

## First calls

1. `myco hunger` — reports substrate hunger and suggests next actions.
2. `myco immune` — runs the lint kernel over this substrate.
3. `myco senesce` — at session close (PreCompact), assimilates raw
   notes + reruns immune with `--fix`. `myco senesce --quick` is the
   reflect-only fallback for the SessionEnd hook (~1.5 s kill budget).

## Subsystems

Each subsystem's doctrine is linked from `_canon.yaml::subsystems`.
Read canon first; everything else is downstream.
